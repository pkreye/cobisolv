#include "ising_graph_helper.h"


int **ising_gh_realize_weights(int *groups, int groups_len, int **weights)
{
    int realized_array[64][ 64];

    int num_spins = groups_len;
    int x, y, i j;

    for (x = 0; x < num_spins; x++) {
        int group_x groups[x];

        size
    }
}
